在设置添加一个类别，管理应用开启行为的

主要添加的功能有
1：自启动：跟随系统自启软件，默认值关
2：静默启动：后台启动，启动的时候不展示窗口，在后台运行，默认值关
3：自动运行：应用打卡的时候自动运行，默认值开


4其他需求
CodexWorkspace.tsx的
原本是固定base_url = "http://127.0.0.1:3000/v1" ，改成了base_url = "${gateway}"，查看相关逻辑gateway是否有相关/v1后缀


5:检查 之前
受点亮模型的影响的内容，

需要检查用到这个端口的配置，在新建配置的时候也正确读取端口信息。，受点亮模型的影响，配置中获取的模型信息也要受点亮模型行为影响下拉选择
另外模型映射也应该按点亮模型选择

另外index.css 在亮色模式下，文字的白色太强，看的不太清晰，调整一下颜色，52525B看看，另外没有点亮的图标文字不发灰了正常显示吧


6：目前卡片url还没有放开url限制
Not allowed to open url https://cc.090668.xyz，支持http和https

